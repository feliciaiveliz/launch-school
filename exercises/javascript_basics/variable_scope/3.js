if (true) {
  let myValue = 20;
}

console.log(myValue);

// Variables declared with 'let' are block scoped
// When we declare the variable myValue within a block,
// it is only accessible within the block
