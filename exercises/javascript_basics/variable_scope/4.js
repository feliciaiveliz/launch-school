function myFunction() {
  let a = 1;

  if (true) {
    console.log(a);
  }
}

myFunction();

// function declaration
// Variables that are declared in an outer scope can be accessed by an inner scope
// Variable 'a' is declared within the function definition
