console.log(greeting);

var greeting = 'Hello world!';

// with hoisting

var greeting;

console.log(greeting);

greeting = 'Hello World!';

// When a variable is declared with 'var' but not assigned a value,
// it is initialized to a value of 'undefined'
