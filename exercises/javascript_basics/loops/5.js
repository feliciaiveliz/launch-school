let array = [];
let index = 0;

while (index < array.length) {
  console.log(array[index]);
  index += 1;
}

// On line 5, we can access each element using bracket notation and each element's index
// We output each value and increment index by 1
// The while loop is terminated after the index i equal to the length of the array, which is 4
